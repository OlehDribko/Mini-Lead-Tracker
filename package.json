{
  "name": "mini-lead-tracker-monorepo",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "install:deps": "npm install",
    "dev:backend": "npm run start:dev --workspace=apps/backend",
    "dev:frontend": "npm run dev --workspace=apps/frontend"
  },
  "workspaces": [
    "apps/*"
  ]
}
